\makeatletter\def\input@path{{minimus}{standalone-silicon}}\makeatother

\documentclass{standalone-silicon}

\usepackage{minimus-text}
\usepackage{minimus-math}
\usepackage{minimus-tikz}

\usepackage{matrix-fig}

\begin{document}
\begin{tikzpicture}

\path (0,0) coordinate (O) ;

\foreach \i in {1,2,...,8}
{
    \foreach \j in {1,2,...,8}
    {
        \path ($(O)+(\i*\xu-\xu,\j*\yu-\yu)$) coordinate (tmp1) ;
        \path ($(O)+(\i*\xu,\j*\yu)$) coordinate (tmp2) ;
        \path (0,0) node[fit=(tmp1)(tmp2),inner sep=0cm,line width=0.0cm] (O\i-\j) {} ;
    }
}

\path ($(O1-8.south west)+(0,\yu)$) coordinate (T) ;

\foreach \i in {1,2,...,8}
{
    \foreach \j in {1}
    {
        \path ($(T)+(\i*\xu-\xu,\j*\yu-\yu)$) coordinate (tmp1) ;
        \path ($(T)+(\i*\xu,\j*\yu)$) coordinate (tmp2) ;
        \path (0,0) node[fit=(tmp1)(tmp2),inner sep=0cm,line width=0.0cm] (T\i-\j) {} ;
    }
}

\foreach \i in {1,2,3}
{
    \draw[rfill] (O\i-\i.north west) rectangle (O\i-\i.south east) ;
}

\foreach \i in {1,2,3}
{
    \draw[bfill] (O\i-\fpeval{\i+1}.north west) rectangle (O\i-8.south east) ;
}

\draw[ghalf] (T1-1.north west) rectangle (T3-1.south east) ;

\draw[mgrid] (O1-1.north west) grid[xstep=\fpeval{abs(\xu)} cm,ystep=\fpeval{abs(\yu)} cm] (O8-8.south east) ;
\draw[mborder] (O1-1.north west) rectangle (O8-8.south east) ;

\draw[mgrid] (T1-1.north west) grid[xstep=\fpeval{abs(\xu)} cm,ystep=\fpeval{abs(\yu)} cm] (T8-1.south east) ;
\draw[mborder] (T1-1.north west) rectangle (T8-1.south east) ;

\foreach \i/\t in {1/1,2/\cdots,3/\cdots,4/k,8/n}
{
    \path (O\i-8.south) node[below] (X\i) {\tickertxt{$\t$}} ;
}

\foreach \i/\t in {1/1,2/\vdots,3/\vdots,4/k,8/n}
{
    \path (O8-\i.east) node[right] (Y\i) {\tickertxt{$\t$}} ;
}

\path ($(O1-1.north)!0.5!(O3-1.north)$) node[above=0.05cm] (Xr13) {\tickertxt{$\sigma$}} ;
\path ($(O5-1.north)!0.5!(O8-1.north)$) node[above=0.05cm] (Xr58) {\tickertxt{$\rho$}} ;

\path ($(O1-1.west)!0.5!(O1-3.west)$) node[left=0.05cm] (Yr13) {\tickertxt{$\sigma$}} ;
\path ($(O1-5.west)!0.5!(O1-8.west)$) node[left=0.05cm] (Yr58) {\tickertxt{$\rho$}} ;

\foreach \ia/\ib in{1/3,5/8}
{
    \draw[tkaux] (O\ia-1.north west) -- ++ (0,0.3) ;
    \draw[tkaux] (O\ib-1.north east) -- ++ (0,0.3) ;
    \draw[tkarr] ([yshift=0.12cm]O\ia-1.north west) -- ([yshift=0.12cm]O\ib-1.north east) ;
}

\foreach \ia/\ib in{1/3,5/8}
{
    \draw[tkaux] (O1-\ia.north west) -- ++ (-0.3,0) ;
    \draw[tkaux] (O1-\ib.south west) -- ++ (-0.3,0) ;
    \draw[tkarr] ([xshift=-0.12cm]O1-\ia.north west) -- ([xshift=-0.12cm]O1-\ib.south west) ;
}

\foreach \i in {1,2,3}
{
    \path[save path=\tmppath] (O\i-\i.north west) rectangle (O\i-\i.south east) ;
    \draw[wclean,use path=\tmppath] ;
    \draw[karea,use path=\tmppath] ;
}

\path[save path=\tmppath] (O1-4.north west) rectangle (O3-4.south east) ;
\draw[wclean,use path=\tmppath] ;
\draw[karea,use path=\tmppath] ;

\path[save path=\tmppath] (T1-1.north west) rectangle (T3-1.south east) ;
\draw[wclean,use path=\tmppath] ;
\draw[garea,use path=\tmppath] ;

\path (O2-2.center) node[] (A1) {\regiontxt{$\vb{A}(i,i)$}} ;
\path (O2-4.center) node[bfill] (A2) {\regiontxt{$\vb{A}(k,i)$}} ;
\path (T2-1.center) node[ghalf] (V) {\regiontxt{$\vb{v}(i)$}} ;

\draw[thick] (A1) -- (A2) ;
\draw[oparr] (A2) -- node[right=0.1cm,infotxt,pos=0.92] (TXT) {$\vb{v}(i)=\vb{A}(k,i)\vb{A}(i,i),\ i=\sigma$} (V) ;

\draw[ultra thin] ($(T1-1.south west)+(-0.5,-0.5)$) rectangle ($(O8-1.north east)+(0.5,0.5)$) ;

\end{tikzpicture}
\end{document}