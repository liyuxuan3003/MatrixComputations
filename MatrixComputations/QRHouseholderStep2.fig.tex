\makeatletter\def\input@path{{minimus}{standalone-silicon}}\makeatother

\documentclass{standalone-silicon}

\usepackage{minimus-text}
\usepackage{minimus-math}
\usepackage{minimus-tikz}

\usepackage{matrix-fig}

\begin{document}
\begin{tikzpicture}

\path (0,0) coordinate (O) ;

\foreach \i in {1,2,...,8}
{
    \foreach \j in {1,2,...,9}
    {
        \path ($(O)+(\i*\xu-\xu,\j*\yu-\yu)$) coordinate (tmp1) ;
        \path ($(O)+(\i*\xu,\j*\yu)$) coordinate (tmp2) ;
        \path (0,0) node[fit=(tmp1)(tmp2),inner sep=0cm,line width=0.0cm] (O\i-\j) {} ;
    }
}

\foreach \i in {1,2,3}
{
    \draw[bfill] (O\i-\fpeval{\i+1}.north west) rectangle (O\i-9.south east) ;
}

\foreach \i in {1,2,3,4}
{
    \draw[rfill] (O\i-\i.north west) rectangle (O8-\i.south east) ;
}

\draw[mgrid] (O1-1.north west) grid[xstep=\fpeval{abs(\xu)} cm,ystep=\fpeval{abs(\yu)} cm] (O8-9.south east) ;
\draw[mborder] (O1-1.north west) rectangle (O8-9.south east) ;

\foreach \i/\t in {1/1,2/\cdots,3/\cdots,4/k,8/n}
{
    \path (O\i-9.south) node[below] (X\i) {\tickertxt{$\t$}} ;
}

\foreach \i/\t in {1/1,2/\vdots,3/\vdots,4/k,9/m}
{
    \path (O8-\i.east) node[right] (Y\i) {\tickertxt{$\t$}} ;
}

\path[save path=\tmppath] (O4-5.north west) rectangle (O4-9.south east) ;
\draw[wclean,use path=\tmppath] ;
\draw[bhalf,barea,use path=\tmppath] ;

\path (O4-7.south) node[bhalf,inner sep=0.06cm,rotate=-90] (A1) {\regiontxt{$\vb{A}(k+1:m,k)$}} ;
\path (O5-2.south) node[rfill,inner sep=0.04cm] (A3) {\regiontxt{$[\vb{v},\beta]=\mathrm{house}(\vb{\vb{A}}(k:m,k))$}} ;

\draw[oparr,latex-] (A1) to[out=100,in=225] node[infotxt,pos=0.65] (TXT) {$\vb{A}(k+1:m,k:n)=\vb{v}(2:k-m+1)$} (A3) ;

\draw[ultra thin] ($(O1-9.south west)+(-0.5,-0.5)$) rectangle ($(O8-1.north east)+(0.5,0.5)$) ;

\end{tikzpicture}
\end{document}